function diagnostics_(auth){if(['SUPER_ADMIN','ORG_ADMIN'].indexOf(String(auth.user.role_code))===-1)throw createAppError_('FORBIDDEN','เฉพาะผู้ดูแลระบบ');const config=getRuntimeConfig_();return{service:config.appName,version:config.version,environment:config.environment,readiness:productionReadinessCheck(),schema:validateSystemSchema(),integrity:verifyDataIntegrity(),triggers:ScriptApp.getProjectTriggers().map(function(t){return{handler:t.getHandlerFunction(),type:String(t.getEventType())};}),checkedAt:new Date().toISOString()};}
